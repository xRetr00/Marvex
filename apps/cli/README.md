# CLI App Placeholder

Status: placeholder.

The CLI is the first client after docs are accepted. It must talk to the Core Service through the approved HTTP API and contracts.

Forbidden now: implementation before docs acceptance.

