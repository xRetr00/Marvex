# Adapters Package Placeholder

Status: placeholder.

This package will eventually hold provider adapters behind ports after docs are accepted, provider contracts are approved in `docs/CONTRACT_APPROVALS.md`, and library decisions are documented.

Forbidden now: implementation files and custom SDKs before library decisions.
