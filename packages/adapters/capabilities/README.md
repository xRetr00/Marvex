# Capability Adapter Seams

Capability adapters expose external protocol or package shapes without owning
CapabilityRuntime policy. The skills adapter is a compatibility surface that
delegates skill primitives to `packages.skills_runtime`.
